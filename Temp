local p = {}

local function getVal(table)
	local root = mw.html.create('div')
	for i, v in pairs(table) do
		local newDiv = root:tag('div')
		newDiv:wikitext(i .. ': ' .. tostring(v))
	end
	
	return tostring(root)
end

function p.test()
	p.xxx = mw.wikibase.getEntityObject(nil)
	
	return p.xxx
end

return p